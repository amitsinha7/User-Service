{
  "info": { "title": "User Services", "description": "API Docs", "version": "1.0.0" },
  "paths": {
    "/v0/users": {
      "get": {
        "summary": "Find all users",
        "description": "Find all users",
        "parameters": [],
        "responses": { "200": { "description": "success" }, "400": { "description": "bad request" }, "401": { "description": "unauthorized, missing/wrong jwt token" } },
        "tags": ["User"],
        "security": [{ "ApiKeyAuth": [] }]
      },
      "post": {
        "summary": "Create a user",
        "description": "Create a user",
        "parameters": [
          {
            "name": "data",
            "description": "request body",
            "schema": { "type": "object", "properties": { "id": { "type": "number", "required": true, "example": 1 }, "name": { "type": "string", "required": true, "example": "Javier" }, "email": { "type": "string", "required": true, "example": "avileslopez.javier@gmail.com" } } },
            "in": "body"
          }
        ],
        "responses": { "200": { "description": "success" }, "400": { "description": "bad request" }, "401": { "description": "unauthorized, missing/wrong jwt token" } },
        "tags": ["User"],
        "security": [{ "ApiKeyAuth": [] }]
      }
    },
    "/v0/users/{id}": {
      "get": {
        "summary": "Find user by id",
        "description": "Find user by id",
        "parameters": [{ "name": "id", "type": "number", "required": true, "description": "id of user", "in": "path" }],
        "responses": { "200": { "description": "success" }, "400": { "description": "bad request" }, "401": { "description": "unauthorized, missing/wrong jwt token" } },
        "tags": ["User"],
        "security": [{ "ApiKeyAuth": [] }]
      },
      "put": {
        "summary": "Update a user",
        "description": "Update a user",
        "parameters": [
          { "name": "id", "type": "number", "required": true, "description": "id of user", "in": "path" },
          {
            "name": "data",
            "description": "request body",
            "schema": { "type": "object", "properties": { "id": { "type": "number", "required": true, "example": 1 }, "name": { "type": "string", "required": true, "example": "Javier" }, "email": { "type": "string", "required": true, "example": "avileslopez.javier@gmail.com" } } },
            "in": "body"
          }
        ],
        "responses": { "200": { "description": "success" }, "400": { "description": "bad request" }, "401": { "description": "unauthorized, missing/wrong jwt token" } },
        "tags": ["User"],
        "security": [{ "ApiKeyAuth": [] }]
      },
      "delete": {
        "summary": "Delete user by id",
        "description": "Delete user by id",
        "parameters": [{ "name": "id", "type": "number", "required": true, "description": "id of user", "in": "path" }],
        "responses": { "200": { "description": "success" }, "400": { "description": "bad request" }, "401": { "description": "unauthorized, missing/wrong jwt token" } },
        "tags": ["User"],
        "security": [{ "ApiKeyAuth": [] }]
      }
    },
    "/testusers": {
      "delete": {
        "summary": "Delete users generated by integration and load tests",
        "description": "Delete users generated by integration and load tests",
        "parameters": [],
        "responses": { "200": { "description": "success" }, "400": { "description": "bad request" }, "401": { "description": "unauthorized, missing/wrong jwt token" } },
        "tags": ["User"],
        "security": [{ "ApiKeyAuth": [] }]
      }
    },
    "/": { "get": { "summary": "Welcome page", "description": "A simple welcome message to verify the service is up and running.", "parameters": [], "responses": { "200": { "description": "success" } }, "tags": ["General"], "security": [{ "ApiKeyAuth": [] }] } }
  },
  "responses": {},
  "definitions": {},
  "tags": [],
  "swagger": "2.0",
  "securityDefinitions": { "ApiKeyAuth": { "type": "apiKey", "in": "header", "name": "Authorization" } }
}
